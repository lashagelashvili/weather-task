export interface CurrenWeathertData {
  icon: string;
  temperature: number;
  weather: string;
  city: string;
  country: string;
}

export interface CurrentDate {
  date: string;
  weekDay: string;
  month: string;
  year: string;
}
